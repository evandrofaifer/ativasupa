name: Manter Supabase Ativo

on:
  schedule:
    - cron: "0 */2 * * *"  # roda a cada 2 horas
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Enviar requisição ao Supabase
        run: |
          curl -I "https://vyjxioxymmxlkzafogrq.supabase.co"
